# Stochastic Trading Frontend

A lightweight static dashboard for the stochastic trading backend.

## Run

From the repo root:

```bash
python -m http.server 5173 --directory frontend
```

Then open `http://localhost:5173`.

## Configure

Update the **API Base URL** input if your backend is not on `http://localhost:8000`.
